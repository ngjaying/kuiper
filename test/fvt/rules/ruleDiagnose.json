{
  "id": "d10001",
  "name": "诊断规则1",
  "sql": "SELECT `ZCU_CANFD2.0x138.DrvrSeatBtnPsd` FROM spiStream WHERE had_changed(true, `ZCU_CANFD2.0x138.DrvrSeatBtnPsd`)",
  "actions": [
    {
      "mqtt": {
        "server": "tcp://127.0.0.1:1883",
        "topic": "result/dia10001",
        "format": "json",
        "sendSingle": true
      }
    }
  ]
}